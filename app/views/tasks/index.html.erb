<h1>Tasks</h1>

<% @tasks.each do |task| %>
<ul>
    <li><%= link_to task.title, task_path(task) %> <p> - </p> <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?"} %>
</li>
</ul>
<%  end  %>

<%= link_to "Add a new task", new_task_path %>



